<%= form_for @venue do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :full_address%>
  <%= f.text_field :full_address %>
  <%= f.select :region_id, Region.all.map{|e| [e.name, e.id]} %>
  <%= f.submit %>
<% end %>
